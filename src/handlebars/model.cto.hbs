// CTO file

namespace com.contractpen

import org.accordproject.cicero.contract.* from https://models.accordproject.org/cicero/contract.cto
import org.accordproject.cicero.runtime.* from https://models.accordproject.org/cicero/runtime.cto

{{#each data.dataModels}}
{{lowercase ciceroAssetOrTransaction}} {{ciceroClassName}}{{#if (equalsLength ciceroExtendsClass 0)}}{{else}} extends {{ciceroExtendsClass}}{{/if}} {
{{#each fields}}
    {{#if isConcept}}
        o {{dataType}} {{fieldName}} optional
    {{/if}}
    {{#if isEvent}}
        o {{dataType}} {{fieldName}}
    {{/if}}
    {{#if isEnum}}
        o {{fieldName}}
    {{/if}}
    {{#if isField}}
        o {{dataType}}{{#if isArray}}[]{{/if}} {{fieldName}}
    {{/if}}
    {{#if isRelationship}}
        --> {{dataType}} {{fieldName}}
    {{/if}}
{{/each}}
}

{{/each}}