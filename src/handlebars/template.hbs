{{#each data}}

// --- new file ---

namespace com.contractpen

{{lowercase ciceroAssetOrTransaction}} {{ciceroClassName}} extends {{ciceroExtendsClass}} {
{{#each fields}}
    o {{dataType}} {{camelcase fieldName}}
{{/each}}
}
{{/each}}

